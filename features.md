# feature-skills-webapp — Features

## In Progress

| Feature | Owner | Notes |
|---|---|---|
| [skills-api-cutover](docs/features/skills-api-cutover/context.md) | Nigel | The arc's last mile: cut the feature-* skills over from writing dev-store HTML to `PUT /api/documents` + fetching `/api/manifests`, run one final import, then retire the dev-store and walker. Needs F1/F2/F3 (all shipped). |

## Available

| Feature | Notes |
|---|---|
| agent-submission-tracker-ops | Split from agent-submission-api (F3, round-1 requirements review): typed tracker mutations (claim/move/ship a feature) and listing of projects/features/documents over the logical-key API. Deferred because writing feature rows directly would race the walker's parse of `features.html` — needs its own design (typed op rewrites the tracker doc body, or tracker-parsing stops being authoritative). Do after agent-submission-api lands. |
| retro-recurrence-trend | Split from retro-findings-capture: surface a finding's recurrence depth ("raised in N retros") as an explicit trend and feed it back into the `/feature-retro` prompt to nudge persistent findings toward becoming tracked features. Design once retro-findings-capture has run in anger. |
| [event-driven-synthesis-wait](docs/features/event-driven-synthesis-wait/context.md) | Replace the skills' 5s busy-poll of `GET /synthesis-response` with an event-driven wait (blocking long-poll backed by the existing broadcaster/SSE), so the agent makes one call, idles silently, and wakes on submission — killing the hourly "still polling" re-announce on long waits. |

## Suggested order

Topological build order per the design doc (§6).

1. `webapp-skeleton` — bones first; everything depends on it
2. `doc-discovery` — filesystem walker + SQLite index
3. `read-state` — stamp last_read_at on every view
4. `inbox-view` — the headline UX
5. `doc-view` — render individual docs in the webapp shell
6. `sse-refresh` — live inbox updates via SSE
7. `synthesis-response-capture` — POST instead of clipboard
8. `comment-capture` — persistent click-to-comment *(folded into `skill-webapp-integration`)*
9. `skill-webapp-integration` — drop Chrome + clipboard from feature-skills; read responses & comments over HTTP

## Done

Completed or obsoleted; kept here for context.

| Feature | Outcome |
|---|---|
| [agent-submission-api](docs/features/agent-submission-api/context.md) | **Shipped.** F3 of the structured-content arc: agents now create/update context/requirements/plan/feedback docs and read comments/synthesis by *logical identity* over HTTP — no HTML files, no `~/.claude` paths. `PUT/GET /api/documents/{project}/{feature}/{doc_type}/{instance}` plus `GET /api/manifests/{doc_type}`. New `storage/documents.py` (`submit_document`/`build_content`/`validate_writable`) reuses the walker's `logical_key` + version-on-change so file-import and API-submit converge on one row (path-less API docs are skipped by the reconcile pass via `NULL NOT IN`); `web/submit.py` adds the handlers — full-replacement writes, `?dry_run` validation, manifest exposure, and comments/synthesis read + integrate by key. Writes are localhost/no-auth (unchanged trust model) and store opaque section bodies in manifest order. Two phase PRs (#38–39) + a review-fix MR (#40: plan manifest gained the long-missing `verification` key; non-string `actor` rejected). Tracker mutations + listing were split out to `agent-submission-tracker-ops`. 592 tests, ruff/ty clean. |
| [flagged-inbox-diff](docs/features/flagged-inbox-diff/context.md) | **Shipped.** The payoff of the structured-content arc: the inbox now flags *why* a doc re-surfaced and opens content changes straight onto a section-aware diff — the original "haven't we done this already?" fix. No migration, no new tables. A new pure `storage/doc_diff.py` extracts tag/comment/whitespace-stripped section text and classifies sections added/removed/changed/unchanged by manifest `key`; `classify_reason` in `inbox.py` maps events newer than the read baseline to a structured `InboxReason` (`created`/`updated`/`reactivated` → content; `comment_*` → "Comments added"; first sighting = no version at-or-before the baseline → "New"; a byte-changed-but-text-identical re-save → "Updated (formatting only)", no diff link). Phase 2 added word-level `difflib` segments (`autojunk=False`) and `render_diff` (each segment `markupsafe.escape()`'d before `<ins>`/`<del>`), surfaced as a `?view=diff` render mode on `/doc/{id}` with a full↔diff toggle and a native+note fallback when there's nothing textual to show; mark-read stays unconditional so the empty-diff case never strands a misleading baseline. Phase 3 deep-links content-change cards to the defaulted diff. Baselines come from new `read_state.last_read_at` + `versions.content_at_or_before` accessors. Three phase PRs (#33–35) + one review-fix MR (#36: shared `humanise_section_key` so the card label and diff heading can't drift; toggle keyed off resolved mode); two clean review passes. 522 tests, ruff/ty clean. |
| [retro-findings-capture](docs/features/retro-findings-capture/context.md) | **Shipped.** `/feature-retro`'s discussion-class findings now persist in the webapp so the continuous-refinement loop compounds. Migration `0004` adds `retro_runs` + `retro_findings`; status (open/actioned/deferred/rejected) and recurrence are separate axes — recurrence is a self-referential `recurs_from` star to the canonical original, `recurrence_count` a COUNT. P1: `POST /retro-findings` (idempotent replace-by-run-key, project resolved by name, feature as a run-level tag, self-run/cross-project `recurs_from` rejected with 400) and `GET /retro-findings?project=` (open+deferred, returns ids for the read→write round trip). P2: `POST /retro-findings/{id}/status` (no-op writes no event; audited via the `events` table) and a "Process findings" panel on the project page (recurrence badge, fetch-only status buttons on the existing SSE reload). Localhost/unauth trust boundary unchanged; the recurrence-trend signal split to `retro-recurrence-trend`. Two phase MRs (#31, #32) + a review-round test for the SET-NULL recurrence branch; zero CI-fix churn. Producer side (`/feature-retro` emitting a run key, querying priors, posting at end) lands in the feature-skills repo. |
| [server-rendered-docs](docs/features/server-rendered-docs/context.md) | **Shipped.** F2 of the structured-content arc: docs now render server-side from F1's stored content — the `/doc/{id}/raw` iframe is gone and comments & synthesis are first-class native webapp UI. A new pure `storage/doc_render.py` holds the render logic (separate from F1's importer parser): `render_section_doc` emits each stored section body in manifest order, `extract_safe_inner` is the single render-safe chokepoint that strips `<head>`/`<style>`/`<script>` from whole-file opaque bodies (tracker), and (P2) `parse_feedback_items` extracts the per-item structure from the opaque feedback body so the webapp renders its own response/flag widgets. `ManifestSpec` gained ordered `(key, label)` `section_labels` (`expected_keys` derived) as the single render/TOC source; the presentation layer relocated into webapp-owned `web/static/doc.{css,js}`; `doc_shell` renders natively with a raw-render fallback for unversioned docs and prefills comments/synthesis from existing rows. Trust model is accident-prevention (trusted self-authored corpus); manifest *exposure* to external agents deferred to `agent-submission-api`. Two phase MRs (P1 #29 + P2 direct-to-main); no new tables, no migration; plan held with zero CI-fix churn. |
| [versioned-content-store](docs/features/versioned-content-store/context.md) | **Shipped.** F1 of the structured-content arc: document content now lives in the DB as versioned, structured sections via a re-runnable importer. The walker (`walker.py`) parses context/requirements/plan into ordered sections (feedback & the tracker ride as opaque whole-bodies) using a new pure `doc_content.py` (manifest-driven, `convert_charrefs=False` for faithful bodies), and records a content snapshot in a new `document_versions` table — cutting a version only on real content change, so no-op re-saves stop surfacing in the inbox. Documents gained a path-independent `logical_key` (migration 0003 + an idempotent startup backfill that dedupes collisions, repointing read-state/synthesis-responses/comments onto the survivor) decoupling identity from `source_path` for the future agent-submission API; archival became a status change on one row. `content_html` was deliberately left NULL so rendering stays disk-based — the version accessor is the seam F2 will consume. Three phase PRs (#26–28) + one direct-to-main, test-only review round; 372 tests, ruff/ty clean. |
| [extra-pages](docs/features/extra-pages/context.md) | **Shipped.** Inbox and navigation extras, all read-model and presentation over the existing schema — no migration. Doc-type badges gained per-type colours (context amber / requirements blue / plan violet / review teal / feedback magenta / shipped green / in-progress grey) via a normalised `badge_kind` slug carried on `InboxCard`, colour *and* label for legibility. A "Mark all read" control on the "New since last visit" header stamps *exactly* that list (reusing `new_since_last_visit`, so "Awaiting your input" is left unread), scoped to the active project filter, via a new `POST /admin/mark-read` and a delegated handler that survives the SSE fragment refetch. New readable pages — per-feature `/project/{p}/feature/{slug}` (status/owner/notes + docs grouped primary / feedback-with-awaiting-badge / archived) and per-project `/project/{p}` (features grouped In progress / Available / Done, plus the tracker doc) — keyed by name+slug with 404-on-unknown, URL-encoded links, and SSE live-refresh that suppresses the initial connect message to avoid a reload loop. Inbox cards were restructured into per-element links (per-document sections open the doc from the feature name; per-feature sections link to the feature page; project names link to the project page) and a "Home" breadcrumb root was added across the doc view and both new pages. Four phase PRs (#22–25) + one direct-to-main review-fix round (Proposal A inbox-link semantics, "Home" crumb, `_rank`→`doc_type_rank` dedup, read-state regression tests); 295 tests, ruff/ty clean. |
| [skill-webapp-integration](docs/features/skill-webapp-integration/requirements.md) | **Shipped.** The skill-side counterpart to `synthesis-response-capture`, completing the clipboard→HTTP transition across both repos. The webapp gained a comment capture/read/integrate HTTP surface (`web/comments.py`: doc-id-keyed `POST /doc/{id}/comments` with replace-active-set + a `comment_submitted` events row + SSE broadcast; path-keyed `GET /comments` active-only with a `submitted` flag; `POST /comments/integrate` mark-by-id, doc-scoped), folding in the never-built `comment-capture` (#8) against the pre-existing empty `comments` table — no migration. The `doc-view` shell reads the iframe's `window.__fsComments` (a one-line backwards-compatible addition to the feature-skills spine templates, since a lexical `const` isn't a window property) and submits via a prominent bottom action-bar CTA; the footer "Copy" button was demoted to a secondary control. On the feature-skills side, all five `google-chrome` opens were dropped for the inbox, and the four review skills now poll `/synthesis-response` and fetch/integrate `/comments` over HTTP (force-walk → 5 s poll → clipboard fallback, defined once in `docs/webapp-polling.md`), retiring both clipboard round-trips. Five phase PRs across two repos (webapp #20–21, skills #1–4) + one direct-to-main review-fix round over two passes — the load-bearing fix: the skills were sending unexpanded `~` paths (and then literal `$HOME` inside single-quoted `-d` bodies) that never matched the walker's absolute `source_path`, silently 404ing the whole HTTP handoff. 255 tests, ruff/ty clean. |
| [synthesis-response-capture](docs/features/synthesis-response-capture/requirements.md) | **Shipped.** Answer a synthesis (feedback) doc in the webapp instead of the copy-to-clipboard / paste-into-chat round-trip. The walker now indexes feedback docs (which carry no `feature-doc-type` meta tag) via a filename-typing fork in `_process_file`, giving them a synthetic `<phase>-feedback` type. A new `web/synthesis.py` exposes `POST /doc/{id}/synthesis-response` (validate shape/integer-keys/string-values/1 MB before the transaction, atomic replace-on-submit into the pre-existing `synthesis_responses` table, broadcast on commit) and `GET /synthesis-response?path=<source_path>` (keyed by the dev-store path the agent already holds, with a `submitted` flag distinguishing unknown-doc from not-yet-answered). A fourth inbox category, "Awaiting your input" (active feedback docs with no submission yet), renders first and excludes those docs from "New since last visit"; submitting live-updates the open tab via the existing SSE broadcaster. Submission reads the same-origin, non-sandboxed feedback iframe's DOM directly from the `doc-view` shell — no `postMessage` bridge and zero feature-skills template change (a deliberate, signed-off divergence from the requirements); feedback docs are kept out of sibling-nav. The skill-side consumer of the read endpoint and clipboard retirement are deferred to `skill-integration-parallel`; the clipboard stays as a fallback until then. Four phase PRs (#16–19) + one direct-to-main review-fix round (reject non-string response values with 400); 215 tests, ruff/ty clean. |
| [sse-refresh](docs/features/sse-refresh/requirements.md) | **Shipped.** Live inbox updates via server-sent events — the open tab refreshes itself when docs land, change, or ship, no manual reload. A new in-process `web/broadcaster.py` holds one `asyncio.Queue` per connected tab; the single serialised walk worker signals it *from the event loop after the threaded walk commits* (the load-bearing cross-thread constraint), whenever a walk produced a net change. `WalkSummary` gained a `shipped` count and a `changed` gate so done-transitions broadcast on their own terms rather than riding an incidental `updated`. A `/events` endpoint (`sse-starlette` `EventSourceResponse`) emits a contentless `changed` message on connect and on every change, holds no DB connection, and deregisters each client in a `finally`. Phase 2 split the inbox card region into an `_inbox_body.html` partial served as a `?fragment=1` no-store, project-filter-preserving fragment, plus a vanilla `EventSource` client that refetches-and-swaps `#inbox-body` on any message (250 ms debounce; refetch-on-connect unifies live updates with stale-tab re-sync, `visibilitychange` as belt-and-braces — no HTMX). Two phase PRs (#14, #15) + one direct-to-main review-fix round; 177 tests, ruff/ty clean. |
| [doc-view](docs/features/doc-view/requirements.md) | **Shipped.** Renders an individual doc inside the webapp shell, closing the inbox loop inbox-view/read-state left open. A new `web/doc_view.py` with two id-keyed routes: `/doc/{id}` (a thin Jinja shell — breadcrumbs from one `documents`→`projects` LEFT-JOIN-`features` lookup, "View source" hatch, read-state stamped once on shell render via the existing `mark_read`) and `/doc/{id}/raw` (serves the indexed file's bytes — id-lookup only, no path-traversal surface, prefers `content_html` as the Stage-2 seam). The doc renders verbatim in a no-sandbox, full-height internal-scroll `<iframe>` so its own CSS/JS (TOC, highlighting, click-to-comment) run intact — the iframe-passthrough choice over inlining (deferred to Stage 2) and document_id routing over readable URLs. Inbox cards with a `document_id` became accessible `aria-label`'d links; graceful 404 / "no longer available" (missing) / 503 states; tracker (`project / Tracker`) and archived docs render but aren't surfaced. Phase 2 added sibling prev/next nav across a feature's active docs ordered by a single `DOC_TYPE_ORDER`. Two follow-up fixes for back-button stale read-state (`Cache-Control: no-store` + bfcache `pageshow` reload). Two phase PRs + one post-merge review-fix round (fold feature-id into the join, no-store test, dedup the Tracker literal); 156 tests, ruff/ty clean. |
| [inbox-view](docs/features/inbox-view/context.md) | **Shipped.** The headline UX — a cross-project inbox Home page replacing the placeholder at `/`. A read-model layer (`storage/inbox.py`: connection-taking `new_since_last_visit` / `in_progress` / `recently_shipped` + `build_inbox`, mirroring `read_state.py`) derives three categories: New since last visit (the unread predicate, INNER-JOINed to features so the null-feature tracker doc and missing/archived docs drop out, uncapped, newest-first), In progress (`features.status='in_progress'`), and Recently shipped (capped+30-day-windowed). Ship time comes from a new `shipped` event the walker emits on the done-transition (`document_id=NULL`, `{project,slug}` payload — matching the design's anticipated sse "ship" event) rather than the unreliable `features.updated_at`. Server-rendered Jinja, no HTMX; not-configured / all-empty / populated states (`Inbox.is_empty`); per-project filter chips; humanised type labels; display-only cards (click-through + read-stamp deferred to `doc-view`, "Awaiting your input" deferred to `synthesis-response-capture`). Three phase PRs + one post-merge review-fix round (empty-state branch collapse, deterministic tie-ordering); 134 tests, ruff/ty clean. |
| [read-state](docs/features/read-state/context.md) | **Shipped.** First writer of the `read_state` table (no migration): a `storage/read_state.py` with `mark_read` (idempotent upsert through `transaction()`), `mark_all_read` (stamps every active doc in a project), and `unread_document_ids` — the inbox's "new since last visit" predicate (active docs with an event newer than `last_read_at`; `COALESCE(…, '')` so a missing row = never-read; strict `>` so ties read as read; optional project filter). A shared `now_iso()` helper in `db.py`, adopted by the walker too, guarantees `last_read_at` and `events.created_at` stay byte-comparable. The bulk action is exposed as name-keyed `POST /admin/projects/{project}/mark-read` (404/503 guards); the per-render stamp is deferred to `doc-view`. Two phase PRs + one post-merge review-fix round (equal-timestamp tie test); 101 tests, ruff/ty clean. |
| [doc-discovery](docs/features/doc-discovery/context.md) | **Shipped.** Filesystem walker indexing the dev-store into the §4 SQLite schema: migration 0002 (DROP+recreate `documents` with `project_id`, nullable `feature_id`, a `status` column — active/archived/missing — and a partial unique index on `source_path`); a synchronous mtime+size-gated `walk()` with status transitions and an `events` row (carrying a `payload_json`) per change; a single serialised async walk worker fed by startup reconcile, an on-demand `POST /admin/discover`, and a `watchfiles` watch; plus `features.html` tracker parsing into `features.status`/`owner`/`notes`. Three phase PRs + one post-merge review-fix round; 85 tests, ruff/ty clean, CI added. |
| [webapp-skeleton](docs/features/webapp-skeleton/context.md) | **Shipped.** Supervised Starlette server on `127.0.0.1:8800` with a Jinja placeholder page and a DB-backed `/healthz` readiness check; migrated SQLite carrying the full §4 schema (per-request connections, WAL, `events` SET-NULL audit semantics, FK indexes); systemd user unit with a working crash-loop cap; kea-style test harness (xdist + pytest-socket, per-worker DB). Three phase PRs + one review-fix round; 27 tests, ruff/ty clean. |
